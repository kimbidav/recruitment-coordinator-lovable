/**
 * Bump to send everyone through onboarding again (e.g. after the setup
 * steps change). Stored per user in agent_settings.onboarding_version when
 * they finish the wizard; the dashboard redirects anyone below it.
 */
export const ONBOARDING_VERSION = 2;
