// The implementation lives with the other shared pure modules so the edge
// functions and the dashboard run the same code. Re-exported here so existing
// imports keep working.
export * from "@shared/pure/companyMatch";
