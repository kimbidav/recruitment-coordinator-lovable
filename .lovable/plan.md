
The Railway backend URL is `https://ashby-automation-production.up.railway.app` (configured in `src/lib/ashbyAutomation.ts`). The frontend calls it directly from the browser via `POST /api/extract`.

Let me verify connectivity from the sandbox.
